# Data-pipeline dependencies (src/data_pipeline).
# Core checks run on the standard library alone; these improve/extend them.

ftfy>=6.0            # D1: robust mojibake repair (fix_encoding.py)
lxml>=5.0            # D3/D5: BPMN XML mutation & building (inject_defects.py, ir_to_bpmn.py)
SpiffWorkflow>=3.0   # D5: BPMN 2.0 XSD schema validation (bpmn_validate.py, validate_rows.py C2 gate)
pyarrow>=15.0        # D5: read the PET parquet for the NL→BPMN bootstrap (pet_to_ir.py)

# Future — configurable rule validation for C2 rows (TDD §4). Installed via npm, not pip:
#   npm i -g bpmnlint
